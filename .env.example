# SPY TA Tracker - Environment Configuration Template
# Copy this file to .env and customize values for your environment

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# SQLite (Default - good for quick testing)
# DATABASE_URL=sqlite:///./spy_tracker.db

# PostgreSQL (Recommended for development and production)
# Local development with Docker (default in start.sh)
DATABASE_URL=postgresql+psycopg2://spy:pass@127.0.0.1:5433/spy

# PostgreSQL - Manual installation
# DATABASE_URL=postgresql+psycopg2://spy:pass@localhost:5432/spy

# PostgreSQL - Production (use environment-specific credentials)
# DATABASE_URL=postgresql+psycopg2://username:password@host:port/database

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

APP_NAME=SPY TA Tracker
DEBUG=true
TIMEZONE=America/Chicago
SYMBOL=SPY

# CORS - Frontend origin(s) allowed to access API
# Development: Use * to allow any origin
# Production: Use specific domain like https://your-domain.com
FRONTEND_ORIGIN=*

# ============================================================================
# AI/OPENAI CONFIGURATION
# ============================================================================

# OpenAI API Key (required for AI predictions)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# AI Model Configuration
OPENAI_MODEL=gpt-5
OPENAI_REASONING_EFFORT=minimal
OPENAI_MAX_COMPLETION_TOKENS=600
OPENAI_TEMPERATURE=0.2
OPENAI_TEXT_VERBOSITY=low

# Historical data lookback for AI context (days)
AI_LOOKBACK_DAYS=5

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================

# Server ports (used by start.sh)
API_PORT=8000
PORT=3000

# ============================================================================
# PRODUCTION OVERRIDES (uncomment for production)
# ============================================================================

# DEBUG=false
# FRONTEND_ORIGIN=https://your-production-domain.com
# DATABASE_URL=postgresql+psycopg2://prod_user:secure_password@prod_host:5432/spy_prod?sslmode=require

# ============================================================================
# DOCKER COMPOSE VARIABLES
# ============================================================================

# These are used by docker-compose.yml when running containerized stack
# They default to the values above but can be overridden

# POSTGRES_USER=spy
# POSTGRES_PASSWORD=pass  
# POSTGRES_DB=spy

# ============================================================================
# SECURITY NOTES
# ============================================================================

# 1. Never commit this file with real credentials to version control
# 2. Use strong passwords in production
# 3. Enable SSL for production database connections
# 4. Restrict FRONTEND_ORIGIN in production
# 5. Consider using environment-specific .env files:
#    - .env.development
#    - .env.staging  
#    - .env.production

# ============================================================================
# QUICK START EXAMPLES
# ============================================================================

# Example 1: Local development with automatic Postgres
# DATABASE_URL=postgresql+psycopg2://spy:pass@127.0.0.1:5433/spy
# OPENAI_API_KEY=sk-your-key
# Then run: ./start.sh

# Example 2: SQLite for quick testing
# DATABASE_URL=sqlite:///./spy_tracker.db
# OPENAI_API_KEY=sk-your-key
# Then run: ./start.sh

# Example 3: Docker Compose full stack
# Set OPENAI_API_KEY, then run: docker-compose --profile full up